applySpecVersion: 1
spec:
  configSync:
    enabled: true
    sourceFormat: unstructured
    syncRepo: https://github.com/600lyy/Build-KRM-Platform
    syncBranch: main
    secretType: token
    policyDir: cluster
  policyController:
    enabled: false 
